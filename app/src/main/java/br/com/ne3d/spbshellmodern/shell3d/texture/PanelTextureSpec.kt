package br.com.ne3d.spbshellmodern.shell3d.texture

/** Shared capture and fallback texture dimensions. Kept modest for the target older GPU. */
object PanelTextureSpec {
    const val WIDTH = 384
    const val HEIGHT = 540
}
